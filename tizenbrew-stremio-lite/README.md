# TizenBrew Stremio Lite (legado)

A pasta `tizenbrew-stremio-lite/` foi mantida como referência da implementação anterior.

A versão ativa para execução no TizenBrew agora é a pasta `app/`, definida em `package.json` via `appPath: "app/index.html"`.
